{
  "entities": {
    "Subject": {
      "title": "Subject",
      "description": "Represents a single academic subject for attendance tracking.",
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "The name of the subject."
        },
        "attended": {
          "type": "number",
          "description": "The number of classes attended."
        },
        "missed": {
          "type": "number",
          "description": "The number of classes missed."
        },
        "userId": {
          "type": "string",
          "description": "The ID of the user who owns this subject."
        }
      },
      "required": ["name", "attended", "missed", "userId"]
    },
    "Integration": {
      "title": "Integration",
      "description": "Stores credentials for a third-party integration, like Google.",
      "type": "object",
      "properties": {
        "accessToken": {
          "type": "string",
          "description": "The OAuth2 access token."
        },
        "refreshToken": {
          "type": "string",
          "description": "The OAuth2 refresh token."
        },
        "expiresAt": {
          "type": "number",
          "description": "The timestamp when the access token expires."
        }
      },
      "required": ["accessToken", "refreshToken", "expiresAt"]
    },
    "StudyMaterial": {
        "title": "Study Material",
        "description": "Represents an uploaded study document.",
        "type": "object",
        "properties": {
            "fileName": {
                "type": "string",
                "description": "The name of the uploaded file."
            },
            "downloadUrl": {
                "type": "string",
                "description": "The public URL to download the file from Firebase Storage."
            },
            "courseId": {
                "type": "string",
                "description": "The ID of the course this material belongs to."
            },
            "userId": {
                "type": "string",
                "description": "The ID of the user who uploaded this material."
            }
        },
        "required": ["fileName", "downloadUrl", "courseId", "userId"]
    }
  },
  "auth": {
    "providers": ["password"]
  },
  "firestore": {
    "/subjects/{subjectId}": {
      "schema": {
        "$ref": "#/entities/Subject"
      },
      "description": "Stores the attendance tracking subjects for all users."
    },
    "/users/{userId}/integrations/{integrationId}": {
      "schema": {
        "$ref": "#/entities/Integration"
      },
      "description": "Stores third-party integration credentials for a user."
    },
    "/study_materials/{materialId}": {
        "schema": {
            "$ref": "#/entities/StudyMaterial"
        },
        "description": "Stores references to uploaded study materials."
    }
  }
}
